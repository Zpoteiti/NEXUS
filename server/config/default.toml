bind_addr = "0.0.0.0:7878"
postgres_dsn = "postgres://user:pass@127.0.0.1:5432/nexus"
vlm_endpoint = "http://100.80.10.33:1234/v1/chat/completions"

[limits]
max_connections = 500
request_timeout_ms = 30000
max_inflight_requests = 2000

[auth]
# Replace placeholder credentials before deploying.
token_issuer = "nexus"
audience = "nexus-nodes"
node_auth_token = "dev-token"
admin_username = "change-me-admin-user"
admin_password = "change-me-admin-password"
